{
  "name": "redux-packed-observable-middleware",
  "version": "0.0.1",
  "description": "Pack middleware for redux-observable",
  "repository": "iamawebgeek/redux-packed",
  "license": "Apache 2.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "rollup -c",
    "prettier": "prettier --write src/** src/**/*",
    "prepublish": "yarn build"
  },
  "files": [
    "dist"
  ],
  "peerDependencies": {
    "redux-observable": "*",
    "redux-packed": "^0.0.1",
    "rxjs": "*"
  }
}
